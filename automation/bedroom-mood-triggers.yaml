- alias: "Mark's Bedroom Sleep Time"
  trigger:
    - platform: time
      at: '03:00:00'
  action:
    - service: input_select.select_option
      data:
        entity_id: input_select.marks_bedroom_select
        option: "Sleep"

- alias: "Mark's Bedroom Off Time"
  trigger:
    - platform: time
      at: '09:00:00'
  action:
    - service: input_select.select_option
      data:
        entity_id: input_select.marks_bedroom_select
        option: "Off"
