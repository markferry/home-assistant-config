hue_library_off:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.library_mode
        option: "Off"

hue_library_cosy:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.library_mode
        option: "Cosy"

hue_library_bright:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.library_mode
        option: "Bright"

hue_library_read:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.library_mode
        option: "Read"

