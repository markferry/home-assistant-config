hue_bedroom_annex2_thermostat:
  sequence:
    - service: climate.set_temperature
      data_template:
        temperature: "{{ requested_level }}"
        entity_id: climate.bedroom_annex2_thermostat

