hue_lounge_off:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.lounge_mode
        option: "Off"

hue_lounge_cosy:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.lounge_mode
        option: "Cosy"

hue_lounge_bright:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.lounge_mode
        option: "Bright"

hue_lounge_movie:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.lounge_mode
        option: "Movie"

