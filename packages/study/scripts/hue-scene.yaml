hue_study_off:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.study_select
        option: "Off"

hue_study_work:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.study_select
        option: "Work"

hue_study_relax:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.study_select
        option: "Relax"

