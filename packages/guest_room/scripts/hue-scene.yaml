hue_guest_room_off:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.guest_room_select
        option: "Off"

hue_guest_room_sleep:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.guest_room_select
        option: "Sleep"

hue_guest_room_cosy:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.guest_room_select
        option: "Cosy"

