hue_bedroom_mark_off:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.bedroom_mark_mode
        option: "Off"

hue_bedroom_mark_sleep:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.bedroom_mark_mode
        option: "Sleep"

hue_bedroom_mark_play:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.bedroom_mark_mode
        option: "Play"

