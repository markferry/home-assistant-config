#
# Devices exposed to voice control platforms
#
google_assistant:
  entity_config:
    media_player.outside_amplifier:
      name: "Outside Amplifier"
      <<: &ga_outside_expose
        expose: true
        room: "Outside"

    media_player.outside_mpd:
      name: "Outside Music Player"
      <<: *ga_outside_expose
