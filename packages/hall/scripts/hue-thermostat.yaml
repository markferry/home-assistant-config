hue_annex_hall_thermostat:
  sequence:
    - service: climate.set_temperature
      data_template:
        temperature: "{{ requested_level }}"
        entity_id: climate.annex_hall_thermostat

hue_hall_thermostat:
  sequence:
    - service: climate.set_temperature
      data_template:
        temperature: "{{ requested_level }}"
        entity_id:
          - climate.hall_east_thermostat
          - climate.hall_west_thermostat

