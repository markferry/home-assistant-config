hue_kitchen_off:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.kitchen_select
        option: "Off"

hue_kitchen_cosy:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.kitchen_select
        option: "Cosy"

hue_kitchen_bright:
    sequence:
    - service: input_select.select_option
      data:
        entity_id: input_select.kitchen_select
        option: "Bright"


